<% title "Mitgliederbereich - Hauptseite" %>
<% include_css "members/main" %>
<p><b>Hallo <%= @_member.nickname %>!</b></p>
<p>Auf dieser Seite findest du ab sofort alle vereinsinternen Informationen, zusätzlich zu den üblichen e-mails. Vorerst beschränkt sich dies auf unsere Probentermine und Downloads. In den kommenden Monaten werden wir diesen Bereich aber weiter ausbauen.</p>
<div class="trenner"></div>
<div class="hl">Probentermine</div>
<%= render "new_button", :action => "Neuer Termin", :url => new_members_date_path, :admin_required => true %>
<table class="rounded">
	<tr>
		<th>Datum</th>
		<th>Uhrzeit</th>
		<th>Betroffene Rollen / Szenen</th>
		<th>Ort</th>
		<% if @_member.admin? %><th></th><% end %>
	</tr>
	<%= content_tag_for :tr, @dates do |date| %>
		<td><%= l date.datetime, format: "%A, %d. %B" %></td>
		<td><%= date.datetime.strftime("%H.%M") %> Uhr</td>
		<td><%= date.info %></td>
		<td><%= date.location %></td>
		<%= content_tag :td, :class => :actions do %>
			<%= render "admin_actions", :obj => date, :delete_confirm => "diesen Termin" %>
		<% end if @_member.admin? %>
	<% end %>
</table>
<div class="export">
	<% webcal_url = members_dates_url(:format => :ics, :protocol => "webcal") %>
	Abonnieren in: <%= link_to "iCal", webcal_url %> | <%= link_to "Outlook", webcal_url %> | 
	<%= link_to "Google Calendar", "https://www.google.com/calendar/render?cid=" + webcal_url, :target => "_blank" %>
</div>
<div class="trenner"></div>
<div class="hl">Downloads</div>
<%= render "new_button", :action => "Neue Datei", :url => new_members_file_path, :admin_required => true %>
<table class="rounded files">
	<%= content_tag_for :tr, @files do |file| %>
		<td><%= image_tag "misc/files/#{File.extname(file.path)[1..-1]}.png", :alt => :pdf %></td>
		<td>
			<%= file.title %>
			<div class="small">
				<%= file.description %>
			</div>
		</td>
		<td class="download"><%= link_to "download", file.path, :target => "_blank" %></td>
		<%= content_tag :td, :class => :actions do %>
			<%= render "admin_actions", :obj => file, :delete_confirm => "diese Datei" %>
		<% end if @_member.admin? %>
	<% end %>
</table>