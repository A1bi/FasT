.other-events
  - %w[].each do |identifier|
    - next if @event&.identifier == identifier
    p Informationen zu #{image_tag "theater/#{identifier}/ticket_header.svg", height: 50, valign: 'middle'} finden Sie #{link_to 'hier', info_identifier_path(identifier)}.
