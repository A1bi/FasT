<thead>
  <tr>
    <th colspan="2"><%= title %></th>
  </tr>
</thead>
<tbody>
  <% overview.each do |row| %>
    <tr>
      <td class="label"><%= row[0] %>:</td>
      <td><%= raw row[1].presence || content_tag(:em, "nicht angegeben", class: :small) %></td>
    </tr>
  <% end %>
</tbody>