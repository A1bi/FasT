form action=ticketing_orders_path method='GET'
  table.rounded.entries.details.search
    tr
      th Bestellung suchen
      th
        = text_field_tag :q, params[:q], placeholder: current_user.admin? ? 'Bestell-, Ticketnummer oder Name des Bestellers' : 'Bestell- oder Ticketnummer'
        =< submit_tag 'suchen', name: nil
