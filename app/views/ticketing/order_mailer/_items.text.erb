Ihre bestellten Artikel:

<%= render 'tickets_table' %>
<%= render 'coupons_table' %>
<%= render 'billing' if local_assigns[:billing] %>

<% if attach_tickets? %>
<% if @order.covid19? %>

<%= render 'covid19' %>
<% end %>

<%= render 'tickets_notes' %>
<% end %>

<%= render 'coupons_notes' if attach_coupons? %>
