wir möchten Sie darauf hinweisen, dass <% if @order.cancelled? %>Ihre<% else %>ein Teil Ihrer<% end %> <%= order_with_number(@order) %> aus folgendem Grund storniert wurde: <%= @reason.present? ? @reason : "<em>Kein Grund angegeben.</em>".html_safe %>

Die stornierten Artikel verlieren damit ihre Gültigkeit. Bei weiteren Fragen können Sie sich gerne an uns wenden.

<%= render 'items', introduction: 'Folgende nicht stornierte Artikel sind weiterhin gültig:' unless @order.cancelled? %>


Mit freundlichen Grüßen
