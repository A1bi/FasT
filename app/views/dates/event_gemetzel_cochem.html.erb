<% title 'Der Gott des Gemetzels – Termine' %>
<% include_css 'dates' %>
<% cache [@event, @ticket_types, @event.on_sale?, Ticketing::Ticket.all] do %>
  <script type="application/ld+json">
    <%= raw render('structured_data.json', image: 'theater/gemetzel/title.svg',
                                           location_type: 'EventVenue',
                                           location_name: 'Kulturzentrum Kapuzinerkloster',
                                           location_address: 'Klosterberg 5, 56812 Cochem') %>
  </script>

  <%= render 'other_events' %>

  <div class="gemetzel">
    Unser Kammerensemble führt <b>„Der Gott des Gemetzels“</b> als Gastspiel im <b>Kulturzentrum Kapuzinerkloster Cochem</b> an folgendem Tag auf:

    <%= render 'dates' %>
    <%= render 'prices' %>

    <% if @event.on_sale? %>
      <%= render 'order_link' %>
      <div class="stores">
        Alternativ erhalten Sie Ihre Tickets auch in der Buchhandlung Layaa-Laulhé in Cochem oder über unsere Hotline (siehe weiter unten).
      </div>
    <% end %>

    <%= render 'questions' %>
  </div>
<% end %>
