div data={ controller: "#{type}-map", "#{type}-map-vendor-stylesheet-path-value": asset_path('mapbox-gl.css'), "#{type}-map-info-path-value": info_path } class=local_assigns[:class]
  .map data-faq-map-target='map'
    .popup data-faq-map-target='popup'
