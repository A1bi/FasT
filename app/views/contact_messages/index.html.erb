<% title "Kontakt" %>
<% include_css "contact_messages" %>
<div class="impressum">
  <div class="hl">Kontakt</div>

  <%= render 'application/main_address' %>

  <div class="trenner"></div>

  <h2>Kontaktformular</h2>

  Sie haben eine Frage zu unserem Verein, den Theaterstücken oder Ihren gebuchten Tickets? Möchten Sie uns Feedback zu unseren Aufführungen geben?
  <p>Teilen Sie uns Ihr Anliegen gerne über folgendes Kontaktformular mit und wir werden uns schnellstmöglich bei Ihnen melden.</p>

  <%= form_for @message do |f| %>
    <%= render 'errors', errors: @message.errors %>
    <table>
      <tr>
        <td>Ihr Name:</td>
        <td><%= f.text_field :name, required: true %></td>
      </tr>
      <tr>
        <td>Ihre E-Mail-Adresse:</td>
        <td><%= f.email_field :email, required: true %></td>
      </tr>
      <tr>
        <td>Ihre Telefonnummer:</td>
        <td><%= f.telephone_field :phone, placeholder: 'falls Sie einen Rückruf wünschen' %></td>
      </tr>
      <tr>
        <td>Betreff:</td>
        <td><%= f.text_field :subject, required: true %></td>
      </tr>
      <tr>
        <td>Nachricht:</td>
        <td>
          <%= f.text_area :content, required: true %>
          <%= honeypot_field %>
        </td>
      </tr>
    </table>
    <div class="submit">
      <%= f.submit value: 'absenden' %>
    </div>
  <% end %>
</div>
